[project]
name = "video-compositor-research"
version = "0.1.0"
description = "Autonomous video composition parameter optimization (fork of autoresearch)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
